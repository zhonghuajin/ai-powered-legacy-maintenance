New templates must implement the following:

1. The `prepare_prompt` interface.
2. The `generate_prompt` interface.
3. Add `#AI will modify codes` at the beginning of the file (if automatic code modification is needed).